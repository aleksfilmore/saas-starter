// Minimal legacy Pages Router error page to satisfy Next.js default export requirements.
// This prevents INVALID_DEFAULT_EXPORT during the "Collecting page data" step.
import React from 'react'

export default function ErrorPage() {
	return null
}
